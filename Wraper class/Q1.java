import java.util.*;

public class Q1 {
    public static void main(String[] args) {
        
        ArrayList<Integer> list = new ArrayList<>();
        list.add(10);
        list.add(20);

        System.out.println(list);
    }
}
// Java’s Collections Framework (like ArrayList) can only store objects, not primitive types (int, char, etc.).
// Primitives are not objects, so you must use wrapper classes (Integer, Character, etc.)
